services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_PASSWORD: uniora
      POSTGRES_DB: uniora
    ports:
      - "55432:5432"
    volumes:
      - uniora-postgres-data:/var/lib/postgresql/data

volumes:
  uniora-postgres-data:
